{
  "name": "ecnu-timetable-calendar",
  "version": "1.9.0",
  "private": true,
  "description": "Export ECNU timetables to desktop calendars or import them on iPhone and Android by QR code.",
  "scripts": {
    "test": "node tests/schedule-core.test.js && node tests/mobile-payload.test.js && node scripts/validate-project.js && node --check api-hook.js && node --check content.js && node --check popup.js && node --check lib/schedule-core.js && node --check lib/mobile-payload.js && node --check mobile/mobile.js",
    "package": "sh scripts/package-extension.sh"
  },
  "license": "MIT",
  "devDependencies": {
    "lz-string": "^1.5.0",
    "qrcode-generator": "^1.4.4"
  }
}
